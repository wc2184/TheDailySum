## Getting started

1. Copy `.env.example` to `.env` and set `VITE_DAILY_WORKER_URL` to your deployed Cloudflare Worker URL (leave blank if you only want scheduled runs).
2. Install dependencies with `npm install`.
3. Start the dev server with:
   ```bash
   npm run dev
   ```
